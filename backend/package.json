{
  "name": "backend-inner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix .. run start",
    "dev": "npm --prefix .. run dev",
    "test-seed": "npm --prefix .. run test-seed"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
